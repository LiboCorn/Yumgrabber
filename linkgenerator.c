#include"linkgen.h"
INFECTEDLINK *il = { 0 };
WEBHOOK wh = "webhook link here"
INFECTEDLINK il = GenerateNewLink((CHAR*)GetListOfLinks());
for (;;) {
PrintOn(il);
if!(il == INVALID_LINK) {
break;
}
}
